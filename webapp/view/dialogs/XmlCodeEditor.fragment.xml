<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:codeeditor="sap.ui.codeeditor"
    xmlns:odpu="de.kernich.odpu.control"
>
    <Dialog
        title="{i18n>dlg.title.metadata}"
        contentHeight="100%"
        contentWidth="100%"
        busyIndicatorDelay="0"
    >
        <customHeader>
            <OverflowToolbar>
                <Title text="{i18n>dlg.title.metadata}" />
                <ToolbarSpacer />
                <SegmentedButton
                    width="250px"
                    selectedKey="{dialog>/viewMode}"
                >
                    <items>
                        <SegmentedButtonItem
                            text="{i18n>btn.code}"
                            key="xml"
                            icon="sap-icon://source-code"
                        />
                        <SegmentedButtonItem
                            text="{i18n>btn.preview}"
                            key="entity"
                            press="onEntityPreview"
                            icon="sap-icon://document-text"
                        />
                    </items>
                </SegmentedButton>
            </OverflowToolbar>
        </customHeader>
        <content>
            <Page showHeader="false">
                <codeeditor:CodeEditor
                    editable="false"
                    type="xml"
                    value="{dialog>/xml}"
                    syntaxHints="false"
                    visible="{= ${dialog>/viewMode} === 'xml'}"
                />
                <IconTabBar
                    selectedKey="{dialog>/selectedType}"
                    expandable="false"
                    visible="{= ${dialog>/viewMode} === 'entity'}"
                    upperCase="true"
                    headerMode="Inline"
                >
                    <items>

                        <IconTabFilter
                        text="All ({= ${dialog>/entities}.length + ${dialog>/functions}.length + ${dialog>/actions}.length })"
                        key="all"
                        icon="sap-icon://list"
                    >
                        <content>
                            <odpu:EntityPreview
                                entities="{dialog>/entities}"
                            />
                            <odpu:FunctionImportPreview
                                functions="{dialog>/functions}"
                            />
                            <odpu:ActionPreview
                                actions="{dialog>/actions}"
                            />
                        </content>
                    </IconTabFilter>
                        <IconTabFilter
                            text="Entitäten ({= ${dialog>/entities}.length })"
                            key="entities"
                            icon="sap-icon://database"
                        >
                            <content>
                                <odpu:EntityPreview
                                    entities="{dialog>/entities}"
                                />
                            </content>
                        </IconTabFilter>

                        <IconTabFilter
                            text="Function Imports ({= ${dialog>/functions}.length })"
                            key="functions"
                            icon="sap-icon://syntax"
                        >
                            <content>
                                <odpu:FunctionImportPreview
                                    functions="{dialog>/functions}"
                                />
                            </content>
                        </IconTabFilter>

                        <IconTabFilter
                            text="Actions ({= ${dialog>/actions}.length })"
                            key="actions"
                            icon="sap-icon://play"
                        >
                            <content>
                                <odpu:ActionPreview
                                    actions="{dialog>/actions}"
                                />
                            </content>
                        </IconTabFilter>
                    </items>
                </IconTabBar>
            </Page>
        </content>
        <buttons>
            <Button
                text="{i18n>btn.download}"
                icon="sap-icon://download"
                press="onDownload"
            />
            <Button
                text="{i18n>btn.copy}"
                icon="sap-icon://copy"
                press="onCopy"
            />
            <Button
                text="{i18n>btn.close}"
                icon="sap-icon://decline"
                press=".onCancel"
            />
        </buttons>
    </Dialog>
</core:FragmentDefinition>
