<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:codeeditor="sap.ui.codeeditor"
    xmlns:odpu="de.kernich.odpu.control"
>
    <Dialog
        title="{i18n>dlg.title.metadata}"
        contentHeight="100%"
        contentWidth="100%"
        busyIndicatorDelay="0"
    >
        <customHeader>
            <OverflowToolbar>
                <Title text="{i18n>dlg.title.metadata}" />
                <ToolbarSpacer />
                <SegmentedButton
                    width="200px"
                    selectedKey="{dialog>/viewMode}"
                >
                    <items>
                        <SegmentedButtonItem
                            text="{i18n>btn.code}"
                            key="xml"
                        />
                        <SegmentedButtonItem
                            text="{i18n>btn.preview}"
                            key="entity"
                            press="onEntityPreview"
                        />
                    </items>
                </SegmentedButton>
            </OverflowToolbar>
        </customHeader>
        <content>
            <Page showHeader="false">
                <codeeditor:CodeEditor
                    editable="false"
                    type="xml"
                    value="{dialog>/xml}"
                    syntaxHints="false"
                    visible="{= ${dialog>/viewMode} === 'xml'}"
                />
                <ScrollContainer visible="{= ${dialog>/viewMode} === 'entity'}" class="sapUiSmallMargin">
                    <VBox>
                        <Panel
                            headerText="Entitäten ({= ${dialog>/entities}.length })"
                            expandable="true"
                            expanded="true"
                            class="sapUiNoContentPadding sapUiSmallMarginBottom"
                        >
                            <odpu:EntityPreview entities="{dialog>/entities}" />
                        </Panel>
                        
                        <Panel
                            headerText="Function Imports ({= ${dialog>/functions}.length })"
                            expandable="true"
                            expanded="true"
                            class="sapUiNoContentPadding sapUiSmallMarginBottom"
                        >
                            <odpu:FunctionImportPreview functions="{dialog>/functions}" />
                        </Panel>
                        
                        <Panel
                            headerText="Actions ({= ${dialog>/actions}.length })"
                            expandable="true"
                            expanded="true"
                            class="sapUiNoContentPadding"
                        >
                            <odpu:ActionPreview actions="{dialog>/actions}" />
                        </Panel>
                    </VBox>
                </ScrollContainer>
            </Page>
        </content>
        <buttons>
            <Button
                text="{i18n>btn.download}"
                icon="sap-icon://download"
                press="onDownload"
            />
            <Button
                text="{i18n>btn.copy}"
                icon="sap-icon://copy"
                press="onCopy"
            />
            <Button
                text="{i18n>btn.close}"
                icon="sap-icon://decline"
                press=".onCancel"
            />
        </buttons>
    </Dialog>
</core:FragmentDefinition>
